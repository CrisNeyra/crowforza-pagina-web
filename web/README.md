# Prototipo deprecado

Esta carpeta (`web/`) es un **prototipo aislado** de formulario → webhook n8n.

**No es la aplicación CROWFORZA.** La fuente de verdad está en la raíz del repo:

- `../index.html`
- `../js/main.js`
- `../css/styles.css`

Para desarrollo local de la tienda:

```powershell
cd ..
python -m http.server 3000
```

Se mantiene solo como referencia histórica. No desplegar ni extender este front.
